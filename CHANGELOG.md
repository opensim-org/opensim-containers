This document lists the changes to `opensim-containers` that are
introduced with each new version

- Introduced env. variable OPENSIM_INSTALL in opensim_deps image, used it downstream (@RobertoRoos)
- 
